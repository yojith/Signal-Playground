[
  {
    "type": "function",
    "function": {
      "name": "pitch_shift",
      "description": "Shift audio pitch in semitones",
      "parameters": {
        "type": "object",
        "properties": {
          "semitones": {
            "type": "integer",
            "description": "Pitch shift amount"
          }
        },
        "required": ["semitones"],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "gain",
      "description": "Apply gain multiplier",
      "parameters": {
        "type": "object",
        "properties": {
          "amount": { "type": "number", "description": "Gain multiplier" }
        },
        "required": ["amount"],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "gain_db",
      "description": "Apply gain in decibels",
      "parameters": {
        "type": "object",
        "properties": {
          "amount_db": { "type": "number", "description": "Gain in decibels" }
        },
        "required": ["amount_db"],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "fir_filter",
      "description": "Apply an FIR filter using custom filter coefficients",
      "parameters": {
        "type": "object",
        "properties": {
          "coefficients": {
            "type": "array",
            "description": "FIR filter coefficients",
            "items": {
              "type": "number"
            }
          }
        },
        "required": ["coefficients"],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "rolling_avg",
      "description": "Apply a rolling average (moving average) filter",
      "parameters": {
        "type": "object",
        "properties": {
          "window_size": {
            "type": "integer",
            "description": "Size of the averaging window in samples"
          }
        },
        "required": ["window_size"],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "generate_sines_phase",
      "description": "Generate a sine wave mixture with per-frequency phase offsets",
      "parameters": {
        "type": "object",
        "properties": {
          "frequencies": {
            "type": "array",
            "description": "Frequencies of sine waves (Hz)",
            "items": {
              "type": "number"
            }
          },
          "phases": {
            "type": "array",
            "description": "Phase offsets for each frequency (radians)",
            "items": {
              "type": "number"
            }
          },
          "duration": {
            "type": "number",
            "description": "Duration of signal in seconds"
          },
          "sr": {
            "type": "integer",
            "description": "Sample rate (samples per second)"
          }
        },
        "required": ["frequencies", "phases", "duration", "sr"],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "generate_sines",
      "description": "Generate a normalized sum of several sine waves or a single sine wave",
      "parameters": {
        "type": "object",
        "properties": {
          "frequencies": {
            "type": "array",
            "description": "Frequencies of sine waves (Hz)",
            "items": {
              "type": "number"
            }
          },
          "duration": {
            "type": "number",
            "description": "Duration of signal in seconds"
          },
          "sr": {
            "type": "integer",
            "description": "Sample rate (samples per second)"
          }
        },
        "required": ["frequencies", "duration", "sr"],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "add_sines",
      "description": "Add sinusoidal components to an existing signal",
      "parameters": {
        "type": "object",
        "properties": {
          "freqs": {
            "type": "array",
            "description": "Frequencies of sine waves to add (Hz)",
            "items": {
              "type": "number"
            }
          },
          "amps": {
            "type": "array",
            "description": "Amplitudes corresponding to each frequency",
            "items": {
              "type": "number"
            }
          }
        },
        "required": ["freqs", "amps"],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "generate_noise",
      "description": "Add white Gaussian noise to a signal",
      "parameters": {
        "type": "object",
        "properties": {
          "std": {
            "type": "number",
            "description": "Standard deviation of the Gaussian noise"
          }
        },
        "required": ["std"],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "resample_sinc",
      "description": "Convert the sample rate of a signal. Supports upsampling and downsampling.",
      "parameters": {
        "type": "object",
        "properties": {
          "new_sr": {
            "type": "integer",
            "description": "New sample rate in Hz (samples per second)"
          }
        },
        "required": ["new_sr"],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "sample_count",
      "description": "Return the number of audio samples in the signal.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "sample_rate",
      "description": "Return the sample rate of the signal in Hz.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "duration",
      "description": "Return the duration of the signal in seconds.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "max_volume",
      "description": "Return the maximum absolute volume (amplitude) of the signal.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "max_frequency",
      "description": "Return the strongest frequency component present in the signal.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "top_frequencies",
      "description": "Return the top N frequency components in the signal.",
      "parameters": {
        "type": "object",
        "properties": {
          "top_n": {
            "type": "integer",
            "description": "Number of top frequency components to return"
          }
        },
        "required": ["top_n"],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "truncate",
      "description": "Truncate the signal between start_time and end_time (seconds). Use the optional `step` parameter to skip samples (a hop size in seconds) while truncating.",
      "parameters": {
        "type": "object",
        "properties": {
          "start_time": {
            "type": ["number", "null"],
            "description": "Start time in seconds (optional)"
          },
          "end_time": {
            "type": ["number", "null"],
            "description": "End time in seconds (optional)"
          },
          "step": {
            "type": ["number", "null"],
            "description": "Step size in seconds (optional)"
          }
        },
        "required": [],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "low_pass",
      "description": "Apply a low-pass FIR filter to remove frequencies above the cutoff.",
      "parameters": {
        "type": "object",
        "properties": {
          "cutoff_hz": {
            "type": "number",
            "description": "Cutoff frequency in Hz"
          },
          "num_taps": {
            "type": "integer",
            "description": "Number of FIR filter taps"
          }
        },
        "required": ["cutoff_hz"],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "high_pass",
      "description": "Apply a high-pass FIR filter to remove frequencies below the cutoff.",
      "parameters": {
        "type": "object",
        "properties": {
          "cutoff_hz": {
            "type": "number",
            "description": "Cutoff frequency in Hz"
          },
          "num_taps": {
            "type": "integer",
            "description": "Number of FIR filter taps"
          }
        },
        "required": ["cutoff_hz"],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "generate_square",
      "description": "Generate a normalized sum of several square waves or a single square wave.",
      "parameters": {
        "type": "object",
        "properties": {
          "frequencies": {
            "type": "array",
            "description": "Frequencies of square waves (Hz)",
            "items": {
              "type": "number"
            }
          },
          "duration": {
            "type": "number",
            "description": "Duration of signal in seconds"
          },
          "sr": {
            "type": "integer",
            "description": "Sample rate (samples per second)"
          }
        },
        "required": ["frequencies", "duration", "sr"],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "generate_sweep",
      "description": "Generate a sine frequency sweep from a starting frequency to an ending frequency.",
      "parameters": {
        "type": "object",
        "properties": {
          "start_freq": {
            "type": "number",
            "description": "Starting frequency in Hz"
          },
          "end_freq": {
            "type": "number",
            "description": "Ending frequency in Hz"
          },
          "duration": {
            "type": "number",
            "description": "Duration of the sweep in seconds"
          },
          "sr": {
            "type": "integer",
            "description": "Sample rate (samples per second)"
          },
          "sweep_type": {
            "type": "string",
            "description": "Sweep type, either 'linear' or 'logarithmic'",
            "enum": ["linear", "logarithmic"]
          }
        },
        "required": ["start_freq", "end_freq", "duration", "sr"],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "add_square",
      "description": "Add square-wave components to an existing signal.",
      "parameters": {
        "type": "object",
        "properties": {
          "freqs": {
            "type": "array",
            "description": "Frequencies of square waves to add (Hz)",
            "items": {
              "type": "number"
            }
          },
          "amps": {
            "type": "array",
            "description": "Amplitudes corresponding to each square wave frequency",
            "items": {
              "type": "number"
            }
          }
        },
        "required": ["freqs", "amps"],
        "additionalProperties": false
      }
    }
  }
]
